# Happy Boutique de Festas

Catálogo e orçamento via WhatsApp.

Desenvolvimento: `npm install` e `npm run dev`.

Vercel: importe este repositório com raiz `./`. O arquivo `vercel.json` configura `npm run build` e saída `dist`. Não são necessárias variáveis de ambiente.
